# App Service (ClusterIP) - app-lb 의 IP TargetGroup 이 이 Pod 들을 등록한다.
apiVersion: v1
kind: Service
metadata:
  name: wsc-svc
  namespace: wsc
spec:
  type: ClusterIP
  selector:
    app: wsc-deploy
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
